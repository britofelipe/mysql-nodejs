<h1>Edit your book register</h1>
<form action="/books/updatebook" method="POST" class="form">
    <input type="hidden" name="id" value="{{book.id}}">
    <div class="form-control">
        <label for="title">Title:</label>
        <input type="text" name="title" placeholder="Write the title of the book" value="{{book.title}}">
    </div>
    <div class="form-control">
        <label for="author">Author:</label>
        <input type="text" name="author" placeholder="Write the name of the author" value="{{book.author}}">
    </div>
    <div class="form-control">
        <label for="pages">Pages:</label>
        <input type="number" name="pages" placeholder="Write the number of pages" value="{{book.pages}}">
    </div>
    <input class="register-button" type="submit" value="Edit">
</form>
<div class="links">
    <a href="/books">List of books</a>    
</div>
